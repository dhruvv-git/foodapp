package routes

import (
	"github.com/gin-gonic/gin"
)

func RestaurantRoutes(incomingRoutes *gin.Engine) {
	// 	incomingRoutes.GET("/restaurants", controller.GetRestaurants())
	// 	incomingRoutes.GET("/restaurants/:resataurant-id", controller.GetRestaurant())
	// 	incomingRoutes.POST("/restaurants", controller.CreateRestaurant())
	// 	incomingRoutes.PATCH("/restaurants/:restaurant-id", controller.UpdateRestaurant())
}
